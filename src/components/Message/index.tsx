import React, { useState, useEffect } from 'react'
import classNames from 'classnames'
import { PROJECT_NAME } from './../../config/constance'
import './message.less'

export const Message: React.FC = () => {
  const notice = () => {

  }
  
  const classString = classNames({
    [`${PROJECT_NAME}-comp-msg`]: true,

  })
  return (
    <div>
      
    </div>
  )
}
